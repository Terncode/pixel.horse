.form-inline.p-1
	.form-group
		a.btn.btn-default(routerLink="/")
			fa-icon([icon]="homeIcon")
	.form-group
		label.control-label.ms-2 horizontal:
		.btn-group.dropdown.ms-1(dropdown)
			button.btn.btn-default.dropdown-toggle(dropdownToggle style="width: 140px;")
				| {{horizontal}}
			.dropdown-menu(*dropdownMenu)
				a.dropdown-item(*ngFor="let f of fields" (click)="horizontal = f; redraw();") {{f}}

	.form-group
		label.control-label.ms-2 vertical:
		.btn-group.dropdown.ms-1(dropdown)
			button.btn.btn-default.dropdown-toggle(dropdownToggle style="width: 140px;")
				| {{vertical}}
			.dropdown-menu(*dropdownMenu)
				a.dropdown-item(*ngFor="let f of fields" (click)="vertical = f; redraw();") {{f}}

	.form-group
		label.control-label.ms-2 coat:
		color-picker.color-picker-inline.ms-1([(color)]="coat" (colorChange)="redraw()")

	.form-group
		label.control-label.ms-2 hair:
		color-picker.color-picker-inline.ms-1([(color)]="hair" (colorChange)="redraw()")

	.form-group
		label.control-label.ms-2 just head
		check-box.check-box-inline.ms-1([(checked)]="justHead" (checkedChange)="redraw()")

	.form-group
		label.control-label.ms-2
		.btn-group.dropdown.ms-1(dropdown)
			button.btn.btn-default.dropdown-toggle(dropdownToggle)
				| &times;{{scale}}
			.dropdown-menu(*dropdownMenu)
				a.dropdown-item((click)="scale = 1; redraw()") &times;1
				a.dropdown-item((click)="scale = 2; redraw()") &times;2
				a.dropdown-item((click)="scale = 3; redraw()") &times;3
				a.dropdown-item((click)="scale = 4; redraw()") &times;4

canvas(#canvas)
